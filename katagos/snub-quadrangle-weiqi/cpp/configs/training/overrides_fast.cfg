# Fast-iteration overrides: load after training/selfplay.cfg or training/match.cfg (later -config wins).
# Tunes visits / cheap search / lead estimation for more games per wall-clock (weaker labels than a "refine" profile).
#
# Example:
#   .\scripts\training_loop.ps1 -SelfplayOverridesPath 'cpp\configs\training\overrides_fast.cfg'
#
# If you pass -SelfplayExtraConfigs manually, include every override file you need (later entries override earlier ones).

maxVisits = 400
reducedVisitsMin = 40
estimateLeadProb = 0.07

# maxVisits = 900
# cheapSearchVisits = 350
# cheapSearchProb = 0.55
# reducedVisitsMin = 280
# estimateLeadProb = 0.08
