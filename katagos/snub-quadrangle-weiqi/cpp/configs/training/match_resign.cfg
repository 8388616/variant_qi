# Optional fourth -config for katago match without -output-dir (play-only benchmarks).
# Not loaded by training_loop.ps1 (match always uses -output-dir + match_training_fast.cfg).

allowResignation = true
resignThreshold = -0.99
resignConsecTurns = 3
